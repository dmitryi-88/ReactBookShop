function cartReducer(state, action) {
    switch (action.type) {
        case "ADD":
            return
        
        case "REMOVE":
            return

        case "UPDATE_QUANTITY":
            return

        case "CLEAR":
            return

        default:
            return state
    }
}

export default cartReducer